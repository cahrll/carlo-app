import React from "react"

// shell lives in the org-scoped layout (needs orgId)
const SidebarGroupLayout = ({ children }: { children: React.ReactNode }) => {
  return <>{children}</>
}

export default SidebarGroupLayout
